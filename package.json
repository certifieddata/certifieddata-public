{
  "name": "certifieddata-public",
  "private": true,
  "packageManager": "pnpm@9.15.4",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec -- rm -rf dist"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
